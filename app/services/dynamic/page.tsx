import { title } from "@/components/primitives";

const DynamicSiteSecvices = () => {
    return (
        <section>
            <h1 className={title({ class: "text-center" })}>
                <span className={title({ color: "blue" })}>
                    Dynamic
                </span>
                Pages
            </h1>
        </section>
    );
};

export default DynamicSiteSecvices;
